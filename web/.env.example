# Agents backend feed (CORS-enabled for http://localhost:3100)
NEXT_PUBLIC_AGENTS_HTTP=http://localhost:3101
NEXT_PUBLIC_AGENTS_WS=ws://localhost:3101/stream

# On-chain Arena contract. Leave empty for pure-mock demo mode.
# Set to a deployed 0x… address to enable on-chain verdicts + reveal.
NEXT_PUBLIC_ARENA_ADDRESS=
NEXT_PUBLIC_ARENA_ROUND_ID=1
