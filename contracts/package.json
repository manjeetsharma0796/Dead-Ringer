{
  "name": "dead-ringer-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Smart-contract skeleton for Dead Ringer — an on-chain spot-the-bot trading game on Mantle Sepolia.",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:mantleSepolia": "hardhat run scripts/deploy.ts --network mantleSepolia",
    "export:abi": "hardhat run scripts/exportAbi.ts",
    "seed:round": "hardhat run scripts/seedRound.ts",
    "reveal:round": "hardhat run scripts/revealRound.ts",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17"
  }
}
