node_modules
cache
artifacts
.env
.env.*
!.env.example
typechain-types

# Ephemeral local-node deployment artifacts (the real testnet record IS committed)
deployments/localhost.json
deployments/round-*.json
